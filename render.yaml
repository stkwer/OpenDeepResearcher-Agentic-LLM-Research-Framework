services:
  - type: web
    name: opendeepresearcher
    env: python
    runtime: python3.11
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app.py --server.port $PORT --server.address 0.0.0.0